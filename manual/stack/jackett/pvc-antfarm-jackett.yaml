apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: localfs-jackett
spec:
  resources:
    requests:
      storage: 5Gi
  accessModes:
    - ReadWriteMany
  storageClassName: local-storage
  volumeName: localfs-jackett
