apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis-cachet
  labels:
    app: redis-cachet
spec:
  strategy:
    type: Recreate
  replicas: 1
  selector:
    matchLabels:
      app: redis-cachet
  template:
    metadata:
      labels:
        app: redis-cachet
    spec:
      nodeSelector:
        dfl.mn/datacentre: digitalocean
      containers:
        - name: redis
          image: redis:latest
          ports:
          - containerPort: 6379
          volumeMounts:
          - mountPath: "/data"
            name: redis-cachet
      volumes:
        - name: redis-cachet
          persistentVolumeClaim:
            claimName: redis-cachet
