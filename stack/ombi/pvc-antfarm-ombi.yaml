apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-localfs-ombi
spec:
  resources:
    requests:
      storage: 5Gi
  accessModes:
    - ReadWriteMany
  storageClassName: local-storage
  volumeName: pv-localfs-ombi
