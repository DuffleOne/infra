apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-localfs-bazarr
spec:
  resources:
    requests:
      storage: 5Gi
  accessModes:
    - ReadWriteMany
  storageClassName: local-storage
  volumeName: pv-localfs-bazarr
